// import { ActionContext } from 'vuex'
// import { SettingsState, Auth, UserAuth } from './types'
// import axios from '@/plugins/axios.ts'
// import storage from '@/helpers/storage'

export default {
  //
  // async AUTHENTICATE(
  // store: ActionContext<SettingsState, any>,
  // auth: Auth
  // ): Promise<UserAuth> {
  // return await axios
  // .post<UserAuth>('/users/authenticate', {
  // username: auth.userName,
  // password: auth.password
  // })
  // .then(res => {
  // console.log(res)
  // store.commit('setAuthUser', res.data)
  // storage.setToken(res.data.token)
  // return res
  // })
  // .catch(error => {
  // console.log(error)
  // return error
  // })
  // }
}
